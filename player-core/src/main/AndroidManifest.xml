<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application>
        <meta-data
            android:name="log_enable"
            android:value="${IS_DEBUG}" />
        <meta-data
            android:name="log_enable_udp"
            android:value="${IS_DEBUG_UDP}" />
        <meta-data
            android:name="log_enable_link_source"
            android:value="${IS_DEBUG_LINK_SOURCE}" />
        <service
            android:name="com.sean.ratel.player.core.data.player.download.VideoDownloadService"
            android:exported="false"
            android:permission="android.permission.FOREGROUND_SERVICE"
            android:foregroundServiceType="dataSync"
            />
        <provider
            android:name="so.smartlab.common.utils.log.RLogInitializer"
            android:authorities="${applicationId}.rlog-init"
            android:exported="false" />

    </application>
</manifest>